# =============================================================================
# SAM3 SageMaker Training Requirements (Minimal)
# =============================================================================
# Only pin essential packages - let pip resolve the rest automatically.
# This prevents pip backtracking which can take hours with exact pins.
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cu124

# -----------------------------------------------------------------------------
# Core ML Frameworks (pinned to specific CUDA version)
# -----------------------------------------------------------------------------
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124

# -----------------------------------------------------------------------------
# SAM3 Training Dependencies
# -----------------------------------------------------------------------------
# Hydra for configuration management (needs specific version for antlr4 compat)
hydra-core>=1.3.0
omegaconf>=2.3.0

# Vision/ML libraries
timm>=1.0.0
einops>=0.8.0
fairscale>=0.4.0
fvcore>=0.1.5

# Image processing
opencv-python>=4.9.0
pillow>=10.0.0
scikit-image>=0.22.0
imageio>=2.30.0

# Data handling
numpy>=1.26.0,<2.0.0
scipy>=1.12.0
scikit-learn>=1.4.0
pandas>=2.0.0

# COCO evaluation
pycocotools>=2.0.0

# HuggingFace (for model downloads) - version pinned at bottom of file
safetensors>=0.4.0

# Logging & tracking
wandb>=0.15.0
tensorboard>=2.15.0
torchmetrics>=1.0.0

# Distributed training
submitit>=1.5.0

# Video decoding
decord>=0.6.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
tabulate>=0.9.0
cloudpickle>=3.0.0
yacs>=0.1.8

# Text processing (required by sam3 tokenizer)
ftfy>=6.0.0
regex>=2023.0.0

# -----------------------------------------------------------------------------
# Zeitview internal packages
# -----------------------------------------------------------------------------
ml-dronebase-data-utils>=0.1.0

# -----------------------------------------------------------------------------
# SageMaker SDK (must match client version exactly to avoid serialization errors)
# -----------------------------------------------------------------------------
sagemaker==2.251.1
protobuf>=3.20.0,<5.0.0

# HuggingFace hub must be <1.0 for transformers/tokenizers compatibility
huggingface_hub>=0.34.0,<1.0.0

